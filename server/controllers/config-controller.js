/*
*   Config endpoints for passing configuration data to the UI
*/

//var configModel = require('../models/config-model');

/*
*   Save an account
*/
/*exports.hubspotConfig = function(req, res, next) {
    configModel.hubspotConfig(function (err, config) {
        if (err) {
            res.send(400, err);
        }
        else {
            res.json(200, config);
        }
    });
};*/