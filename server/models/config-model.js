/*
*   API for configuration settings
*/

var config = require('../../config/config');

/*exports.hubspotConfig = function(callback) {
    callback(null, config.env[process.env.NODE_ENV].hubspotApi);
}*/
